import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: 'Nunito, Montserrat, sans-serif',
    body: 'Nunito, Montserrat, Times New Roman'
  },
});


export default theme;
